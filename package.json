{
  "name": "puppy-training-coach",
  "private": true,
  "version": "1.0.0",
  "description": "A puppy training planner and progress dashboard for building routines, skills, and confidence with a new dog.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev --prefix server\" \"npm:dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run build && npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
